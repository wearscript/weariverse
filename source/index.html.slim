- row_counter = 0
- data.shared.each do |id, app|
  - if row_counter % 3 == 0
    .row
  - row_counter += 1
  .col-md-4
    .app-card
      .panel.panel-primary
        .panel-heading
          h3.panel-title= app.name
        .panel-body= partial :app_card, locals: {app: app}
        .panel-footer
          ul.nav.nav-pills
            li = link_to "Wear", "https://api.picar.us/wearscriptdev/?script=#{app.app_uri}/index.html"
            li = link_to "Source", app.app_uri
